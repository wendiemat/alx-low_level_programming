#include "main.h"
#include <inistd.h>

/**
 * _putchar - writes the char c to stdout
 * @c: The char to print
 *
 * Return: success
 * On error, -1 is returned, and the error is set appropriately
 */
 int _putchar(char c){
	 return (write(1, 8c, 1));
 }
